# 自选股公告雷达（stock_radar）需求说明

## 1. 项目概述

**stock_radar** 是一个面向 A 股散户的自选股公告监控与智能提醒工具。它定期抓取自选股的最新公告，抓取公告**正文**并做**关键词预过滤**得到"关键正文"，交大模型做**结构化（JSON）**摘要与重要性/倾向判断，再按**重要性阈值智能过滤**，去重后通过消息推送到用户手机，帮助用户在海量公告中快速筛出与自己持仓相关的关键信息。

- 运行形态：单次运行的命令行脚本（`python main.py`），适合配合系统定时任务（如 cron / 计划任务）周期性执行。
- 技术栈：Python（≥3.11），数据源为东方财富（经 akshare，已做多渠道抽象便于扩展）；正文经东财正文接口 / 官方 PDF 级联抓取并**本地缓存**、`pypdf`/去标签解析、关键词过滤；摘要用 DeepSeek 大模型（JSON 输出）；推送用 PushPlus；去重用本地 SQLite；日志落控制台 + 滚动文件。

## 2. 目标与价值

- **省时**：自动过滤全市场公告，只保留自选股相关内容。
- **降噪**：先按关键词抽取关键正文再喂 AI；AI 用一句话 + 要点概括，标注重要性（高/中/低）与倾向（利好/利空/中性/关注）。
- **少打扰**：常规低重要性公告（如例行股东大会通知）**不推送**；同一条公告只提醒一次。
- **省流量/省钱**：正文本地缓存，不重复下载；只把关键正文（非全文/非 PDF）送模型。
- **合规**：AI 仅客观描述公告，严禁给出任何投资建议、买卖点或涨跌预测。
- **稳健可观测**：分层异常分类 + 全量日志，单点失败自动降级不中断整体。

## 3. 功能需求

### 3.1 自选股与渠道配置
- `.env` 中 `WATCHLIST` 以英文逗号分隔配置股票代码；`DATA_SOURCE` 选数据渠道（默认 `eastmoney`）。
- `WATCHLIST` 为空时记日志并退出（`ConfigError` 语义）。

### 3.2 公告采集（多渠道可扩展）
- 数据源抽象为 `NoticeSource`，`get_source(name)` 按 `DATA_SOURCE` 选择实现；新增渠道只需继承并登记。当前可选：`eastmoney`（默认）、`cninfo`。
- `EastMoneyNoticeSource`（东财）：按日期拉**全市场**公告再按自选股本地过滤。
- `CninfoNoticeSource`（巨潮资讯，官方法定披露平台，沪深京全覆盖）：**按代码逐只**查询回看窗口内的信息披露公告，链接为巨潮公告详情页。
- 二者共性：`LOOKBACK_DAYS` 控制回看天数；单只/单日失败记日志跳过；按 `(代码,日期,标题)` 内部去重；产出 `Notice`。
- **全失败语义（P0）**：回看窗口内**全部单元**（东财=全部日期 / 巨潮=全部代码）请求均失败时，抛 `DataSourceError`，由 `main` 记错误并退出——**不得**当成「本次没有新公告」。部分失败则 warning 后用成功单元继续；真正无数据（空结果 / 巨潮无公告 KeyError）仍返回空列表。

### 3.3 去重（防重复推送）
- `Notice` 初始化时以 `md5(code|date|title)` 生成 ID；SQLite（`data/pushed.db`，`code`/`date` 建索引）记录已处理公告；仅新 ID 进入后续。

### 3.4 正文抓取与处理（fetch → parse → filter，可开关、带缓存）
由 `FETCH_CONTENT` 控制（默认开）。职责按单一职责拆为四步：
- **`notice_fetcher`（url → html/pdf）**：按 URL 分流，**先查本地缓存**（`data/content_cache/`）命中即用、否则下载并写缓存。缓存键：东财 `ANxxxx`；巨潮 `CN{announcementId}`（或 PDF 路径 stem）。
  - **东财**：提取 `art_code` → ① 正文接口 `np-cnotice-stock.eastmoney.com/api/content/ann`；② 失败/为空 → PDF `pdf.dfcfw.com/.../H2_{art_code}_1.pdf`。
  - **巨潮（cninfo.com.cn）**：① URL 本身为 `static.cninfo.com.cn` PDF 则直下；② 否则拉详情页解析 PDF 链接；③ 再失败则用 `announcementId` + `announcementTime` 拼接 `finalpage/{date}/{id}.PDF` 尝试。全程超时/重试/UA/Referer。
- **`notice_parser`（raw → text）**：PDF 用 `pypdf` 提取；HTML/纯文本去标签、反转义、规整空白。
- **`text_filter`（text → 关键正文）**：按关键词抽取命中的段落/句子（默认关键词见 §7），无命中回退前几段，并截断到 `MAX_CONTENT_CHARS`。
- 任一步失败均 best-effort 降级：正文取不到时摘要环节改用标题。

### 3.5 例行标题预滤（可选，省 token）
- `title_rules.try_routine_summary`：在调用 AI 前用标题正则匹配常见例行公告（股东大会、法律意见书、回购进展、投资者关系活动等）。
- 命中则直接产出 `重要性=低` 的 `Summary`（`content_source=rule`），**不抓正文、不调模型**；随后仍走重要性阈值，默认不推送但会标记已处理。
- `ROUTINE_TITLE_FILTER=false` 可关闭；`ROUTINE_TITLE_PATTERNS` 可覆盖默认正则（逗号分隔）。

### 3.6 AI 摘要（JSON 输出，标题直出）
- 输入 = **公告标题 + 关键正文**（不送全文/不送 PDF）；无正文退标题；正文截断到 `MAX_CONTENT_CHARS`。
- 提示词：A 股公告摘要助手，任务=提取事实/判断重要性/判断倾向；**禁止**投资建议、买卖建议、涨跌预测、"利好买入"类表达。
- 开启 **JSON 模式**，输出并解析为 `Summary`：
  ```json
  {"importance":"高/中/低","sentiment":"利好/利空/中性/关注","summary":"一句话客观摘要","key_points":["要点1","要点2"]}
  ```
- 调用设 **`LLM_TIMEOUT`**（默认同 `REQUEST_TIMEOUT`），客户端与单次 create 均带超时，避免挂死。
- **标题直出**：标题不进 AI 加工，由 `render.py` 原样输出。
- 单条摘要失败兜底为 `重要性=中` 的占位 `Summary`（"摘要失败,建议人工查看原文"），**仍会推送**，避免漏掉重要公告。

### 3.7 智能推送过滤（按重要性阈值）
- `push_policy.should_push(summary)`：重要性低于 `PUSH_MIN_IMPORTANCE`（默认 `中`）的公告**不推送**（如例行股东大会通知）；`中`/`高`（如重大合同）推送；未知重要性放行。
- 被过滤掉的公告**仍标记已处理**，避免下次重复抓取与摘要。

### 3.8 消息推送
- 组装 Markdown：股票名称/代码、公告标题（原文）、`【重要性X | 倾向Y】摘要` + 关键要点列表、日期与原文链接。
- **分页**：`render.paginate_notices` 按 `PUSH_MAX_PER_MESSAGE`（默认 8）与 `PUSH_MAX_CHARS`（默认 12000）拆多条消息；标题带 `(i/n)`。任一分页 `NotifyError` 则整批不标记，下次重试。
- 始终控制台打印；配置 `PUSHPLUS_TOKEN` 时经 PushPlus 以 markdown 模板推送（设超时）。
- 推送**硬失败抛 `NotifyError`**：不标记已推送，下次自动重试。

### 3.9 推送后标记
- 推送成功后写入 SQLite；单条标记失败记日志不中断。

### 3.10 日志与启动软校验
- `setup_logging()`：控制台（stderr）+ `data/stock_radar.log` 滚动文件（1MB×3），级别由 `LOG_LEVEL` 控制。
- 启动时 `_warn_config()`：`DEEPSEEK_API_KEY` / `PUSHPLUS_TOKEN` 为空、非常见 `DATA_SOURCE` 时 **warning 不硬退**（WATCHLIST 空仍 error 退出）。

### 3.11 异常分类
- `StockRadarError` 基类，派生 `ConfigError`/`DataSourceError`/`ContentFetchError`/`SummarizeError`/`NotifyError`/`StorageError`；底层抛具体子类，`main` 分步捕获记日志。

## 4. 处理流程

```
setup_logging → 校验 WATCHLIST → 构造组件(run() 内局部对象)
        ↓
① 采集 → ② 去重(is_new) → 新公告列表(无则退出)
        ↓
③ 富化(每条): 例行标题预滤(命中则跳过后续) → fetcher → parser → text_filter → summarizer(JSON,带超时)
        ↓
④ 智能过滤: push_policy 拆分「推送」/「过滤」; 过滤项直接标记已处理
        ↓
⑤ 推送(达阈值): paginate → 多页 render_blocks → notifier.notify(任页失败整批不标记)
        ↓
⑥ 标记已推送
```

## 5. 模块设计

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| 入口编排 | `main.py` | 串联六步流程；`run()` 内构造组件、分步异常处理 |
| 数据模型 | `models.py` | `Notice`、`RawNotice`、`Summary` dataclass；`make_id` |
| 异常分类 | `exceptions.py` | `StockRadarError` 及各子类 |
| 日志 | `logging_setup.py` | 控制台 + 滚动文件 |
| 配置 | `config.py` | 从 `.env` 加载全部配置（模块级常量） |
| 数据源 | `sources/`（`base`/`eastmoney`/`cninfo`/`__init__`） | `NoticeSource` 抽象 + 东财/巨潮实现 + 注册表工厂 |
| 正文抓取 | `notice_fetcher.py` | url → html/pdf 原始载荷，带本地缓存 |
| 正文解析 | `notice_parser.py` | raw → text（pdf/html） |
| 关键词过滤 | `text_filter.py` | text → 关键正文 |
| 标题预滤 | `title_rules.py` | 例行公告标题 → 低重要性 `Summary`（可关） |
| 摘要 | `summarizer.py` | 标题+关键正文 → JSON `Summary`（`LLM_TIMEOUT`） |
| 推送策略 | `push_policy.py` | `Summary` → 是否推送 |
| 渲染 | `render.py` | Markdown + `paginate_notices` |
| 推送 | `notifier.py` | 控制台 + PushPlus |
| 存储 | `store.py` | SQLite 去重 + 索引，可注入 db_path |

## 6. 数据结构

### 6.1 `Notice`（dataclass）
`code`/`name`/`title`/`date`/`url`；`id`（`md5(code|date|title)`）；`content`（关键正文，可空）；`summary`（`Summary`）。

### 6.2 `RawNotice`（dataclass）
`kind`（`html`/`pdf`）、`text`（html/纯文本载荷）、`data`（pdf 二进制）。

### 6.3 `Summary`（dataclass）
`importance`（高/中/低）、`sentiment`（利好/利空/中性/关注）、`summary`（一句话）、`key_points`（要点数组）、`content_source`（content/title）。

### 6.4 数据库表 `pushed`（`data/pushed.db`）
`id`(PK)、`code`(索引 `idx_pushed_code`)、`title`、`date`(索引 `idx_pushed_date`)、`pushed_at`。

## 7. 配置说明（.env）

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `WATCHLIST` | 空 | 自选股代码，英文逗号分隔（必填） |
| `LOOKBACK_DAYS` | `3` | 公告回看天数 |
| `DATA_SOURCE` | `eastmoney` | 数据渠道：`eastmoney`（东财） / `cninfo`（巨潮资讯） |
| `FETCH_CONTENT` | `true` | 是否抓取正文 |
| `KEYWORDS` | 中标,合同,回购,减持,增持,业绩,诉讼,处罚,并购,重组,分红 | 关键正文抽取关键词（逗号分隔，可覆盖默认） |
| `PUSH_MIN_IMPORTANCE` | `中` | 低于该重要性的公告不推送（低/中/高） |
| `ROUTINE_TITLE_FILTER` | `true` | 是否启用例行标题预滤（跳过 AI） |
| `ROUTINE_TITLE_PATTERNS` | 空=内置默认 | 覆盖预滤正则，逗号分隔 |
| `PUSH_MAX_PER_MESSAGE` | `8` | 单条推送最多公告条数 |
| `PUSH_MAX_CHARS` | `12000` | 单条推送最大字符数（约） |
| `MAX_CONTENT_CHARS` | `3000` | 送模型的关键正文最大字符数 |
| `CONTENT_CACHE_DIR` | `data/content_cache` | 正文本地缓存目录 |
| `REQUEST_TIMEOUT` | `15` | 外部 HTTP 超时（秒） |
| `LLM_TIMEOUT` | 同 `REQUEST_TIMEOUT` | 大模型调用超时（秒） |
| `HTTP_RETRIES` | `2` | 外部请求重试次数 |
| `LOG_LEVEL` | `INFO` | 日志级别 |
| `LOG_FILE` | `data/stock_radar.log` | 日志文件路径 |
| `DEEPSEEK_API_KEY` | 空 | DeepSeek API 密钥 |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | DeepSeek 接口地址 |
| `DEEPSEEK_MODEL` | `deepseek-chat` | 模型名称 |
| `PUSHPLUS_TOKEN` | 空 | PushPlus 推送 Token，为空则只控制台打印 |

## 8. 依赖与运行

- 运行（`requirements.txt`）：`akshare`、`openai`、`python-dotenv`、`requests`、`pypdf`。
- 开发（`requirements-dev.txt`）：`pytest`、`pytest-mock`。
```bash
pip install -r requirements.txt
cp .env.example .env   # 按需填写配置
python main.py
```

### 8.1 定时任务示例（外挂调度）

项目本身是单次 CLI，需系统定时触发。建议交易日盘前/晚间各跑一次；工作目录必须是项目根（以便加载 `.env` 与 `data/`）。

**Windows 任务计划程序（管理员 PowerShell 示例）**

```powershell
# 将路径改成你的 Python 与项目目录
$py = "C:\Users\你\AppData\Local\Programs\Python\Python311\python.exe"
$dir = "D:\work\AIproject\stock_radar"
$action = New-ScheduledTaskAction -Execute $py -Argument "main.py" -WorkingDirectory $dir
# 每天 8:30 与 18:30
$t1 = New-ScheduledTaskTrigger -Daily -At 8:30am
$t2 = New-ScheduledTaskTrigger -Daily -At 6:30pm
Register-ScheduledTask -TaskName "stock_radar" -Action $action -Trigger $t1,$t2 -Description "自选股公告雷达"
```

或图形界面：任务计划程序 → 创建基本任务 → 程序/脚本填 `python.exe`，参数 `main.py`，起始于项目目录。

**Linux / macOS cron**

```cron
# 每天 08:30、18:30（请改成实际路径）
30 8,18 * * * cd /path/to/stock_radar && /usr/bin/python3 main.py >> data/cron.out 2>&1
```

跑完后可看 `data/stock_radar.log`：出现 `DataSourceError` / `推送失败` 即采集或推送异常，勿与「本次没有新公告」混淆。

## 9. 非功能需求

- **健壮性**：采集/正文抓取/解析/摘要/标记单点失败均记日志并降级，不中断整体；推送失败不误标记。
- **幂等/防扰**：SQLite 去重 + 重要性阈值过滤，减少重复与噪声。
- **可观测**：全量日志（控制台 + 滚动文件）+ 异常分类。
- **兼容性**：入口对 Windows 控制台重设 UTF-8 编码。
- **合规性**：AI 输出严格限定客观描述。
- **可配置/可扩展**：配置统一走 `config.py`；数据源可插拔；关键词、阈值可调。

## 10. 测试

- `tests/` 下 pytest 用例，**全部 mock** 网络/LLM，无真实外呼：
  覆盖 `models`、`store`、`sources.*`、`notice_fetcher`、`notice_parser`、`text_filter`、`title_rules`（例行预滤）、`push_policy`、`summarizer`（JSON/超时参数）、`notifier`、`render`（分页）。
- 运行：`pip install -r requirements-dev.txt && pytest -q`。

## 11. 已知限制与可扩展方向

**当前限制**
- 东财正文接口与 PDF 均为未公开端点，`art_code` 提取与解析可能随其变动，已用超时/重试/缓存/降级兜底。
- 巨潮正文依赖详情页 HTML 结构或 `finalpage` 路径约定，站点改版时可能失败并降级为标题摘要。
- 推送渠道、大模型为单一实现；无内置调度，需依赖外部定时任务（见 §8.1）。
- 关键词过滤为字面匹配，可能漏掉同义表达。

**可扩展方向**
- 接入更多数据源与推送渠道；关键词/阈值按行业细化；正文缓存加过期清理。
- 内置调度与失败重试/告警；自选股分组、按板块聚合。

## 开发约束

- Python >= 3.11
- 全项目使用类型注解（type hints）
- 使用 dataclass 定义数据模型
- 不允许使用全局变量
- 配置统一从 config.py 获取
- 所有外部请求必须设置超时
- 所有异常必须记录日志，不得静默忽略
- 新增功能优先保持模块职责单一，不修改 main.py 的整体流程
- AI 输出必须采用 JSON 格式，避免解析不稳定
